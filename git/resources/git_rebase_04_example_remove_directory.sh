git reset HEAD~
# Then add/remove files and make commits
# rm -rf someDirectoryWithBinaries
# git add .
# git commit -m "Old commit message"
# If you are ready continue the rebase
git rebase --continue