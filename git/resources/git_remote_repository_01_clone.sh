# Change the directory to somewhere you want to have the project
# directory like: "$ cd ~/Documents && mkdir StuPro && cd StuPro"

# Now clone the repository (per default into the directory "panorama")
git clone https://gitlab-pas.informatik.uni-stuttgart..../panorama.git
# You need to enter your credentials, because this is a private repository
# Or you do "git clone https://name:password@gitlab-pas......ama.git"

# Configure your account to act not with the global but the ivps authority
git config user.name "Your Name (The one from the GitLab account)"
git config user.email "name@ipvs.uni-stuttgart.de"